steps:
- name: "gcr.io/cloud-builders/gcloud"
  args: [
    "app", 
    "deploy"
  ]
  env:
  - 'PROJECT_ID=$PROJECT_ID'
  - 'SHORT_SHA=$SHORT_SHA'
  - 'BRANCH_NAME=$BRANCH_NAME'
  - 'REPO_NAME=$REPO_NAME'
timeout: "1600s"
